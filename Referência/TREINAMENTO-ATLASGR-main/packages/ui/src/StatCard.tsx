import React from "react";
import { MetricCard } from "./MetricCard";

// Alias StatCard to MetricCard for consistency with the requested spec,
// they serve the same fundamental purpose in the dashboard
export const StatCard = MetricCard;
