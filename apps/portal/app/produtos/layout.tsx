import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Produtos — Portal Total Trac",
};

export default function ProdutosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
