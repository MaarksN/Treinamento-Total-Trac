import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Prova Final — Portal ATLASGR",
};

export default function ProvaFinalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
